import React from 'react'
import styled from 'styled-components'

const Settings = () => {
    return (
        <Container>
            settings is the real settings 
        </Container>
    )
}

export default Settings
 const Container = styled.div`
 color: white;
 `
